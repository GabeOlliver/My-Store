import React from 'react';
import ReactDOM from 'react-dom/client';
import StyledComponentsApp from './04-styled-components/StyledComponentsApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <StyledComponentsApp />
  </React.StrictMode>
);